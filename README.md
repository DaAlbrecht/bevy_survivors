# Bevy Survivors

## Assets setup

Due to different licensing terms, the assets used in this project are not included in the repository. To set up the assets, please follow these steps:

```bash
# New clone
git clone --recurse-submodules git@github.com:DaAlbrecht/bevy_survivors.git

# Existing clone
git submodule update --init --recursive

```
